syntax = "proto3";

package authorization;

enum Roles {
  USER = 0;
  ADMIN = 1;
  MODERATOR = 2;
}

// Input Messages

message RegisterInput {
  string email = 1;
  string password = 2;
  string passwordConfirmation = 3;
  SessionInput session = 4;
}

message LoginInput {
  string email = 1;
  string password = 2;
  SessionInput session = 3;
}

message RefreshInput {
  string refreshToken = 1;
  SessionInput session = 2;
}

message ChangePasswordInput {
  string oldPassword = 1;
  string newPassword = 2;
  string newPasswordConfirmation = 3;
}

message ForgotPasswordInput {
  string email = 1;
}

message ResetPasswordInput {
  string email = 1;
  string token = 2;
  string newPassword = 3;
  string newPasswordConfirmation = 4;
  SessionInput session = 5;
}

message SessionInput {
    string os = 1;
    string device = 2;
    string browser = 3;
    string ip = 4;
}

message ValidateInput {
  string accessToken = 1;
  SessionInput session = 2;
}

// Output Messages

message AuthOutput {
  string accessToken = 1;
  string refreshToken = 2;
}

message UserPayload {
  string id = 1;
  string sessionId = 2;
  Roles role = 3;
}

message UserEntity {
  string id = 1;
  string email = 2;
  Roles role = 3;
  bool isActive = 4;
  string sessionId = 5;
  string sessionName = 6;
}
